# 系统逆向解剖原本 (Reverse Architecture Report)

> **解剖者（AI）声明**：本报告提取自代码库当前的真实物理状态，不包含任何未来的假设与规划。代码即事实。

## 一、 物理拓扑与基础算力耗散 (Physical Topology)

_(提取当前代码库配置的真实运行环境)_

- **运行容器/进程节点**：[例如：通过 docker-compose 发现有 Postgres 16, Redis 7, Backend(Rust), Frontend(Node)]

- **核心第三方依赖网络**：[例如：系统强依赖外部的 MiniMax API、外部的 RSS XML 节点。]

- **环境变量控制面 (Env Vars)**：[列出决定系统生死的核心环境变量，如 `DATABASE_URL`, `MINIMAX_API_KEY`，并指出哪些是强制要求的]


## 二、 领域本体与真理源 (Ontology & Single Source of Truth)

_(基于 DB Migrations 或 ORM 模型提取真实的底层数据结构)_

### 2.1 核心数据表 (Core Tables)

- **表名：`[例如：articles]`**

    - **核心字段**：[仅列出主键、状态字段、大文本字段、外键]

    - **索引策略**：[例如：在 user_id 和 created_at 上有联合索引]

    - **物理事实**：[例如：存在一个 default now() 的时间戳，无物理删除逻辑]

- **表名：`[例如：insights]`**

    - ...


### 2.2 状态驻留点 (State Persistence)

- **数据库 (DB)**：存储了哪些绝对不能丢失的数据？

- **缓存 (Cache/Redis)**：当前代码中，Redis 究竟存储了哪些键值？TTL 设定是多少？（列出真实的 Key Pattern，如 `articles:{user_id}`）

- **向量库 (VectorDB)**：Chroma 中实际存储的 metadata 结构是什么？


## 三、 边界与海关 (API Boundaries)

_(提取系统向外网暴露的真实接口契约)_

### 3.1 鉴权防腐层 (Auth Layer)

- **代码物理实现**：[例如：代码中实际通过验证 Request Header 中的 Authorization Bearer 进行拦截。如果不存在强签名验证，请如实指出其脆弱性。]


### 3.2 核心暴露接口 (Core APIs)

_(仅提取发生状态改变 POST/PUT/DELETE，或消耗大量算力的核心 GET 请求)_

- **`[Method] [Path]`** (如 `POST /api/v1/articles/ingest`)

    - **触发条件**：[例如：前端主动调用或内部定时器触发]

    - **实际入参**：[JSON Schema 结构]

    - **副作用 (Side Effects)**：[执行此接口后，数据库哪张表被写入？调用了哪个外部 API？]


## 四、 核心因果链与暗流 (Causality & Dark Flows)

_(这是解剖的最核心部分。不要写增删改查，写出系统内部的自动化流程)_

### 4.1 异步任务与定时器 (Cron & Background Jobs)

- **任务名称/描述**：[例如：RSS 增量抓取进程]

- **触发频率**：[例如：代码中写死为每 60 分钟执行一次]

- **执行逻辑图**：

    1. 读取 DB 中活跃的 RSS 源。

    2. 并发拉取 XML (配置了 5s 超时)。

    3. 若失败 -> 仅打印 Warn，直接丢弃（不重试）。

    4. 若成功 -> 解析并写入 DB。


### 4.2 第三方 API 调用链 (External API Integration)

- **目标**：[例如：MiniMax 大模型打分]

- **触发节点**：在文章入库后异步触发。

- **真实容错逻辑**：[代码中实际是怎么写的？失败了是重试？还是将状态置为 `FAILED`？还是会导致主线程 Panic？]


## 五、 技术债与硬编码规则 (Technical Debt & Hardcoded Reality)

_(AI 必须在这里指出代码中不优雅、但客观存在的妥协)_

- **硬编码的魔法数字 (Magic Numbers)**：[例如：分页写死为 20，向量匹配 Top K 写死为 5，超时时间写死为 10s]

- **潜在的系统单点/瓶颈**：[例如：Rust 后端在执行大批量 embedding 序列化时可能会阻塞 Tokio 的工作线程]