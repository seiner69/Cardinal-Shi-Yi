# 6-Bit FSM 拓扑引擎 — 设计与代码对齐报告 (V11.0)

**生成时间：2026-04-25**
**版本：V11.0 离散自动机可证伪版**
**检查范围：V11.0设计文档 vs 后端 fsm_kernel.py / api.py / schema.py vs 前端 OverlayUI.tsx / useStore.ts**

---

## 一、V11.0 核心升级项

| V11.0 设计项 | 代码实现 | 状态 |
|---|---|---|
| 离散差分方程 E_i(t) = E_i(t-1) - R_i·α(B_{t-1}) | `fsm_kernel.py:step_energy()` | ✅ |
| 离散差分方程 P_i(t) = P_i(t-1) + C_i·B_up(t-1) | `fsm_kernel.py:step_pressure()` | ✅ |
| α严格定义：α=1.0（支撑），α=2.0（unsupported） | `fsm_kernel.py:alpha()` | ✅ |
| p维度（奇偶共振轴）{-1,1}离散集合 | `fsm_kernel.py:p_dimension()` | ✅ |
| e维度（能量耗散轴）∈ [-1,1] | `fsm_kernel.py:e_dimension()` | ✅ |
| t维度（时间消耗轴）∈ [0,1] | `fsm_kernel.py:t_dimension()` | ✅ |
| 三大维度应力 σ_p, σ_e, σ_t | `fsm_kernel.py:stress_p/e/t()` | ✅ |
| 综合应力 σ = max(σ_p, σ_e, σ_t) | `fsm_kernel.py:max_stress_trigger()` | ✅ |
| Conf_M1 = 1 - max(U_E, U_P, U_R) | `fsm_kernel.py:conf_m1()` | ✅ |
| Monte Carlo扰动引擎（N=1000） | `fsm_kernel.py:monte_carlo_stress()` | ✅ |
| SSOT管线：raw physics → compute all | `fsm_kernel.py:raw_physics_step()` | ✅ |
| 离散熵 S(t) = -∑p_i log(p_i) | `fsm_kernel.py:discrete_entropy()` | ✅ |

---

## 二、路径一：微观单点击穿 (Bit Flip) — ✅ V11.0完整实现

| 设计文档 | V11.0代码实现 | 状态 |
|---|---|---|
| 燃料耗尽触发（1→0断裂）| `check_fuel_exhaustion()` 离散差分 | ✅ |
| 压强击穿触发（0→1爆破）| `check_explosive_rebound()` 离散差分 | ✅ |
| Max-Stress Trigger 唯一动爻 | `max_stress_trigger()` 返回应力最大节点 | ✅ |
| 确定性 Bit Flip | `flip_bit()` 翻转B[i]，重置E_i/E_initial，P_i=0 | ✅ |
| 路径一路由 | `route_evolution_path()` path=1 | ✅ |

**V11.0变化**：从V2.0的连续积分方程改为离散差分方程，增加了p/e/t三维应力分量。

---

## 三、路径二：隐性内核暴露 (Hidden Core Drive) — ✅ 已实现

| 设计文档 | V11.0代码实现 | 状态 |
|---|---|---|
| 互卦演化（内外卦互换）| `path2_hidden_core_exposure()` | ✅ |
| 触发条件：B[1]^B[2]==1 或 B[6]^B[5]==1 | `route_evolution_path()` 路径二判定 | ✅ |
| 路由超时潜伏（T_MASK=5）| `route_evolution_path(time_in_state > T_MASK)` | ✅ |

---

## 四、路径三：宏观热力学衰变 (Macro Entropy Decay) — ✅ 已实现

| 设计文档 | V11.0代码实现 | 状态 |
|---|---|---|
| 熵增DAG单向演化链 | `path3_macro_entropy_decay()` | ✅ |
| Entropy Lock（封闭系统S递增）| `if next_S <= current_S: break` | ✅ |
| 巨型系统判定（M > M_THRESHOLD=4）| `route_evolution_path()` | ✅ |
| 返回完整演化链 | entropy_chain[] 含conf_m1 | ✅ |

---

## 五、路径四：元级别重置与倒置 — ✅ 已实现

| 设计文档 | V11.0代码实现 | 状态 |
|---|---|---|
| 极性反转（错）：6bits全翻 | `path4_meta_reset_or_invert()["错_极性反转"]` | ✅ |
| 参照系倒置（综）：B[1]↔B[6]等 | `path4_meta_reset_or_invert()["综_参照系倒置"]` | ✅ |
| 死锁标志触发 | `route_evolution_path(deadlock_flag=True)` | ✅ |
| 外部降维打击触发 | `route_evolution_path(delta_E_ext < -1000*M)` | ✅ |

---

## 六、HEXAGRAM_LOOKUP 卦名映射 — ✅ 已修复

**已修复**：`"离坤": "否"` → `"离坤": "晋"`（火地晋）

---

## 七、64态节点物理描述表 — ✅ 已落地

| 项目 | 实现 | 状态 |
|---|---|---|
| HEX_STATES字典（64节点）| `fsm_kernel.py` 内置完整状态表 | ✅ |
| get_hex_state()查询 | 返回(index, 状态名, 物理描述) | ✅ |
| fallback未定义组合 | 返回"未定义态" | ✅ |

---

## 八、API层 V11.0升级 — ✅ 完成

| API端点 | V11.0新增字段 | 状态 |
|---|---|---|
| `/api/simulate` | `conf_m1`（系统置信度） | ✅ |
| `/api/evolve` | `conf_m1`（各态）, `unsupported`标志 | ✅ |
| `/api/node` | `E_initial`, `R`, `R_base`, `conf_m1` | ✅ |
| `/api/infer` | deterministic层含conf_m1 | ✅ |

---

## 九、前后端对齐 — ✅ 完整

| 显示项 | 前端字段 | 后端字段 | 状态 |
|---|---|---|---|
| 内/外系统 | fsmData.inner_system / outer_system | FSMOutput.inner_system / outer_system | ✅ |
| 6-Bit机器码 | deterministic.current_node.code | DeterministicResult.current_node.code | ✅ |
| 动爻位置 | deterministic.max_stress_bit | DeterministicResult.max_stress_bit | ✅ |
| 应力类型 | deterministic.stress_type | DeterministicResult.stress_type | ✅ |
| 演化路径 | deterministic.evolution_path | DeterministicResult.evolution_path | ✅ |
| 路径名称 | deterministic.evolution_path_name | DeterministicResult.evolution_path_name | ✅ |
| 6种翻转预览 | deterministic.all_possible_moves | DeterministicResult.all_possible_moves | ✅ |
| 下一状态 | deterministic.next_state | DeterministicResult.next_state | ✅ |
| 系统置信度 | deterministic.current_node.conf_m1 | conf_m1字段 | ✅ |
| 变爻预览 | simulateFlips | /api/simulate flips[] | ✅ |

---

## 十、V11.0可证伪协议支持

| 需求 | 实现 | 状态 |
|---|---|---|
| 系统置信度可量化 | `conf_m1()` ∈ [0,1] | ✅ |
| Monte Carlo扰动采样 | `monte_carlo_stress()` N=1000 | ✅ |
| 三大不确定性维度追踪 | `compute_U()` → U_E, U_P, U_R | ✅ |
| SSOT管线（raw physics输入）| `raw_physics_step()` | ✅ |

---

## 十一、总体结论

```
V11.0 离散自动机内核：
  路径一 Bit Flip（微观单点击穿）  → ✅ 完整实现（离散差分+三维应力）
  路径二 隐性内核暴露             → ✅ 完整实现（互卦+超时潜伏）
  路径三 宏观热力学衰变           → ✅ 完整实现（熵增DAG链）
  路径四 元级别重置与倒置         → ✅ 完整实现（错卦+综卦）
  Conf_M1 置信度                  → ✅ 实现
  Monte Carlo 扰动引擎            → ✅ 实现（N=1000）
  SSOT 管线                       → ✅ 实现

前后端字段对齐                   → ✅ 完整正确
HEXAGRAM_LOOKUP 映射错误         → ✅ 已修复
64态节点物理描述表               → ✅ 已落地
```

**V11.0升级状态：所有设计项均已在代码中实现。**
